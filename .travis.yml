language: python
python:
  - "3.6"
  - "3.7"
install:
  - pip install -r requirements.txt
  - pip install -r requirements_dev.txt
script:
  - cd test && pytest

after_success:
  - pip install coveralls
  - coveralls